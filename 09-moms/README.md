# Övning 9: Beräkna moms (Funktion)

## Instruktioner
- Skapa en funktion `beräknaMoms(pris, momsSats)` som returnerar pris * (1 + momsSats).
- Anropa funktionen med t.ex. `pris=100` och `momsSats=0.25` och skriv ut resultatet.

## Körning
`node index.js`
